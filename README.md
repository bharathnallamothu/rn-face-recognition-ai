steps to run :

yarn 
yarn android

cd android && ./gradlew clean

clone git repo 

in app.tsx 
REFERENCE_IMAGE_URL replace existing s3 with image that you want to compare (source image)

Note: we are using faceNet model. it only works on android